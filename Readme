This HTML and JavaScript-based URL shortener allows users to quickly shorten long URLs and decode them back to their original form. With a straightforward interface, users can enter a long URL, generate a shortened version, and easily copy and share it. Additionally, the tool offers the functionality to decode shortened URLs, restoring them to their original format. As a frontend-only solution, no server-side processing is required, making it easy to deploy and use. 
